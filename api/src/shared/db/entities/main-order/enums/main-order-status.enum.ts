export enum MainOrderStatusEnum {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  CANCELED = 'canceled',
  IN_PROGRESS = 'in-progress',
  COMPLETED = 'completed',
  PARTIAL_CONFIRMED = 'partially-confirmed',
}
